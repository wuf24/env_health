# AMR_AGG_raw_mean X-driven / Nature-like simplified baseline：12 模型逐一详细分析

这里固定围绕 2050 年的未来情景结果解读 12 个归档模型。
每个模型都单独报告变量结构、关键系数、baseline 轨迹、最敏感情景和省级热点，不再只延伸主模型。

## 严筛主模型：SYS_09851

- 识别口径：`Province: No / Year: Yes`，来源为 `systematic`。
- 变量结构：`R1xday | 抗菌药物使用强度 | TA（°C） | PM2.5 | 医疗水平 | 卫生程度（日污水处理能力） | 牲畜饲养-猪年底头数 | 食品消费量`。温度代理为 `TA（°C）`。
- 关键系数：`R1xday` = 0.855，`抗菌药物使用强度` = 0.642，温度代理系数 = 0.472。
- baseline 轨迹：从 31.150 变化到 34.702，期间变化 +3.552；相对最后观测值的差异为 +3.244。
- 情景敏感性：2050 年最强的是“SSP5-8.5（rx1day）”(+0.278)，最弱的是“SSP1-1.9（rx1day）”(+0.150)；情景 spread 为 0.127。
- 省级热点：在最强情景下，差值较高的省份主要是 浙江、广东、天津，差值较低的省份主要是 河南、河北、上海。
- 写作提示：该模型里绝对系数最大的变量是 `医疗水平` (+1.407)，因此正文解释时可以优先说明它与 R1xday 通道的相对权重。

## 严筛模型 2：SYS_10001

- 识别口径：`Province: No / Year: Yes`，来源为 `systematic`。
- 变量结构：`R1xday | 抗菌药物使用强度 | TA（°C） | PM2.5 | 医疗水平 | 饮用水供水综合生产能力(万立方米/日) | 牲畜饲养-猪年底头数 | 食品消费量`。温度代理为 `TA（°C）`。
- 关键系数：`R1xday` = 0.892，`抗菌药物使用强度` = 0.635，温度代理系数 = 0.458。
- baseline 轨迹：从 31.191 变化到 34.429，期间变化 +3.237；相对最后观测值的差异为 +2.971。
- 情景敏感性：2050 年最强的是“SSP5-8.5（rx1day）”(+0.290)，最弱的是“SSP1-1.9（rx1day）”(+0.157)；情景 spread 为 0.133。
- 省级热点：在最强情景下，差值较高的省份主要是 浙江、广东、天津，差值较低的省份主要是 河南、河北、上海。
- 写作提示：该模型里绝对系数最大的变量是 `医疗水平` (+1.300)，因此正文解释时可以优先说明它与 R1xday 通道的相对权重。

## 严筛模型 3：SYS_10002

- 识别口径：`Province: No / Year: Yes`，来源为 `systematic`。
- 变量结构：`R1xday | 抗菌药物使用强度 | TA（°C） | PM2.5 | 医疗水平 | 饮用水供水综合生产能力(万立方米/日) | 牲畜饲养-猪年底头数 | 文盲比例`。温度代理为 `TA（°C）`。
- 关键系数：`R1xday` = 0.915，`抗菌药物使用强度` = 0.669，温度代理系数 = 0.692。
- baseline 轨迹：从 31.272 变化到 34.666，期间变化 +3.394；相对最后观测值的差异为 +3.209。
- 情景敏感性：2050 年最强的是“SSP5-8.5（rx1day）”(+0.297)，最弱的是“SSP1-1.9（rx1day）”(+0.161)；情景 spread 为 0.136。
- 省级热点：在最强情景下，差值较高的省份主要是 浙江、广东、天津，差值较低的省份主要是 河南、河北、上海。
- 写作提示：该模型里绝对系数最大的变量是 `医疗水平` (+1.292)，因此正文解释时可以优先说明它与 R1xday 通道的相对权重。

## 严筛模型 4：SYS_09941

- 识别口径：`Province: No / Year: Yes`，来源为 `systematic`。
- 变量结构：`R1xday | 抗菌药物使用强度 | TA（°C） | PM2.5 | 医疗水平 | 城市用水普及率 | 牲畜饲养-猪年底头数 | PA（%） | 食品消费量`。温度代理为 `TA（°C）`。
- 关键系数：`R1xday` = 0.979，`抗菌药物使用强度` = 0.756，温度代理系数 = 0.511。
- baseline 轨迹：从 31.114 变化到 34.382，期间变化 +3.269；相对最后观测值的差异为 +2.925。
- 情景敏感性：2050 年最强的是“SSP5-8.5（rx1day）”(+0.318)，最弱的是“SSP1-1.9（rx1day）”(+0.172)；情景 spread 为 0.146。
- 省级热点：在最强情景下，差值较高的省份主要是 浙江、广东、天津，差值较低的省份主要是 河南、河北、上海。
- 写作提示：该模型里绝对系数最大的变量是 `医疗水平` (+1.473)，因此正文解释时可以优先说明它与 R1xday 通道的相对权重。

## 严筛模型 5：SYS_09926

- 识别口径：`Province: No / Year: Yes`，来源为 `systematic`。
- 变量结构：`R1xday | 抗菌药物使用强度 | TA（°C） | PM2.5 | 医疗水平 | 城市用水普及率 | 牲畜饲养-猪年底头数 | 食品消费量`。温度代理为 `TA（°C）`。
- 关键系数：`R1xday` = 0.947，`抗菌药物使用强度` = 0.755，温度代理系数 = 0.536。
- baseline 轨迹：从 31.143 变化到 34.588，期间变化 +3.445；相对最后观测值的差异为 +3.131。
- 情景敏感性：2050 年最强的是“SSP5-8.5（rx1day）”(+0.308)，最弱的是“SSP1-1.9（rx1day）”(+0.167)；情景 spread 为 0.141。
- 省级热点：在最强情景下，差值较高的省份主要是 浙江、广东、天津，差值较低的省份主要是 河南、河北、上海。
- 写作提示：该模型里绝对系数最大的变量是 `医疗水平` (+1.453)，因此正文解释时可以优先说明它与 R1xday 通道的相对权重。

## 严筛模型 6：SYS_09927

- 识别口径：`Province: No / Year: Yes`，来源为 `systematic`。
- 变量结构：`R1xday | 抗菌药物使用强度 | TA（°C） | PM2.5 | 医疗水平 | 城市用水普及率 | 牲畜饲养-猪年底头数 | 文盲比例`。温度代理为 `TA（°C）`。
- 关键系数：`R1xday` = 0.981，`抗菌药物使用强度` = 0.775，温度代理系数 = 0.776。
- baseline 轨迹：从 31.220 变化到 34.867，期间变化 +3.647；相对最后观测值的差异为 +3.410。
- 情景敏感性：2050 年最强的是“SSP5-8.5（rx1day）”(+0.319)，最弱的是“SSP1-1.9（rx1day）”(+0.173)；情景 spread 为 0.146。
- 省级热点：在最强情景下，差值较高的省份主要是 浙江、广东、天津，差值较低的省份主要是 河南、河北、上海。
- 写作提示：该模型里绝对系数最大的变量是 `医疗水平` (+1.442)，因此正文解释时可以优先说明它与 R1xday 通道的相对权重。

## 严筛模型 7：SYS_09791

- 识别口径：`Province: No / Year: Yes`，来源为 `systematic`。
- 变量结构：`R1xday | 抗菌药物使用强度 | TA（°C） | PM2.5 | 医疗水平 | 生活垃圾无害化处理率 | 牲畜饲养-猪年底头数 | PA（%） | 食品消费量`。温度代理为 `TA（°C）`。
- 关键系数：`R1xday` = 0.985，`抗菌药物使用强度` = 0.744，温度代理系数 = 0.582。
- baseline 轨迹：从 31.203 变化到 34.897，期间变化 +3.695；相对最后观测值的差异为 +3.440。
- 情景敏感性：2050 年最强的是“SSP5-8.5（rx1day）”(+0.320)，最弱的是“SSP1-1.9（rx1day）”(+0.173)；情景 spread 为 0.147。
- 省级热点：在最强情景下，差值较高的省份主要是 浙江、广东、天津，差值较低的省份主要是 河南、河北、上海。
- 写作提示：该模型里绝对系数最大的变量是 `医疗水平` (+1.440)，因此正文解释时可以优先说明它与 R1xday 通道的相对权重。

## 严筛模型 8：SYS_09776

- 识别口径：`Province: No / Year: Yes`，来源为 `systematic`。
- 变量结构：`R1xday | 抗菌药物使用强度 | TA（°C） | PM2.5 | 医疗水平 | 生活垃圾无害化处理率 | 牲畜饲养-猪年底头数 | 食品消费量`。温度代理为 `TA（°C）`。
- 关键系数：`R1xday` = 0.954，`抗菌药物使用强度` = 0.743，温度代理系数 = 0.605。
- baseline 轨迹：从 31.229 变化到 35.016，期间变化 +3.787；相对最后观测值的差异为 +3.558。
- 情景敏感性：2050 年最强的是“SSP5-8.5（rx1day）”(+0.310)，最弱的是“SSP1-1.9（rx1day）”(+0.168)；情景 spread 为 0.142。
- 省级热点：在最强情景下，差值较高的省份主要是 浙江、广东、天津，差值较低的省份主要是 河南、河北、上海。
- 写作提示：该模型里绝对系数最大的变量是 `医疗水平` (+1.421)，因此正文解释时可以优先说明它与 R1xday 通道的相对权重。

## 稳健性模型 2：SYS_09556

- 识别口径：`Province: No / Year: Yes`，来源为 `systematic`。
- 变量结构：`R1xday | 抗菌药物使用强度 | TA（°C） | PM2.5 | GDP | 城市用水普及率 | 牲畜饲养-猪年底头数 | 主要城市降水量 | 食品消费量`。温度代理为 `TA（°C）`。
- 关键系数：`R1xday` = 1.064，`抗菌药物使用强度` = 0.703，温度代理系数 = 0.318。
- baseline 轨迹：从 31.190 变化到 32.012，期间变化 +0.822；相对最后观测值的差异为 +0.555。
- 情景敏感性：2050 年最强的是“SSP5-8.5（rx1day）”(+0.346)，最弱的是“SSP1-1.9（rx1day）”(+0.187)；情景 spread 为 0.158。
- 省级热点：在最强情景下，差值较高的省份主要是 浙江、广东、天津，差值较低的省份主要是 河南、河北、上海。
- 写作提示：该模型里绝对系数最大的变量是 `GDP` (+1.091)，因此正文解释时可以优先说明它与 R1xday 通道的相对权重。

## 稳健性模型 3：SYS_09557

- 识别口径：`Province: No / Year: Yes`，来源为 `systematic`。
- 变量结构：`R1xday | 抗菌药物使用强度 | TA（°C） | PM2.5 | GDP | 城市用水普及率 | 牲畜饲养-猪年底头数 | 主要城市降水量 | 文盲比例`。温度代理为 `TA（°C）`。
- 关键系数：`R1xday` = 1.075，`抗菌药物使用强度` = 0.718，温度代理系数 = 0.455。
- baseline 轨迹：从 31.163 变化到 31.710，期间变化 +0.547；相对最后观测值的差异为 +0.253。
- 情景敏感性：2050 年最强的是“SSP5-8.5（rx1day）”(+0.349)，最弱的是“SSP1-1.9（rx1day）”(+0.189)；情景 spread 为 0.160。
- 省级热点：在最强情景下，差值较高的省份主要是 浙江、广东、天津，差值较低的省份主要是 河南、河北、上海。
- 写作提示：该模型里绝对系数最大的变量是 `R1xday` (+1.075)，因此正文解释时可以优先说明它与 R1xday 通道的相对权重。

## 稳健性模型 1：方案F_低VIF主线组

- 识别口径：`Province: No / Year: Yes`，来源为 `curated`。
- 变量结构：`TA（°C） | R1xday | PM2.5 | 抗菌药物使用强度 | GDP | 文盲比例 | 人均日生活用水量(升) | 牲畜饲养-猪年底头数 | 主要城市日照时数`。温度代理为 `TA（°C）`。
- 关键系数：`R1xday` = 0.873，`抗菌药物使用强度` = 0.762，温度代理系数 = 0.508。
- baseline 轨迹：从 31.056 变化到 30.791，期间变化 -0.265；相对最后观测值的差异为 -0.666。
- 情景敏感性：2050 年最强的是“SSP5-8.5（rx1day）”(+0.284)，最弱的是“SSP1-1.9（rx1day）”(+0.154)；情景 spread 为 0.130。
- 省级热点：在最强情景下，差值较高的省份主要是 浙江、广东、天津，差值较低的省份主要是 河南、河北、上海。
- 写作提示：该模型里绝对系数最大的变量是 `GDP` (+1.004)，因此正文解释时可以优先说明它与 R1xday 通道的相对权重。

## 主模型：方案A_平衡主线组

- 识别口径：`Province: No / Year: Yes`，来源为 `curated`。
- 变量结构：`省平均气温 | R1xday | PM2.5 | 抗菌药物使用强度 | GDP | 文盲比例 | 人均日生活用水量(升) | 牲畜饲养-猪年底头数`。温度代理为 `省平均气温`。
- 关键系数：`R1xday` = 0.869，`抗菌药物使用强度` = 0.777，温度代理系数 = 0.108。
- baseline 轨迹：从 30.914 变化到 29.421，期间变化 -1.493；相对最后观测值的差异为 -2.036。
- 情景敏感性：2050 年最强的是“SSP5-8.5（rx1day）”(+0.282)，最弱的是“SSP1-1.9（rx1day）”(+0.153)；情景 spread 为 0.129。
- 省级热点：在最强情景下，差值较高的省份主要是 浙江、广东、天津，差值较低的省份主要是 河南、河北、上海。
- 写作提示：该模型里绝对系数最大的变量是 `GDP` (+1.097)，因此正文解释时可以优先说明它与 R1xday 通道的相对权重。
